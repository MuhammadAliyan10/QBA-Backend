"""
Core Browser Module

Deliberately empty to prevent circular import chains.
Import from specific submodules directly:
    from core.browser.session import SessionManager, get_session_manager
    from core.browser.dom_harvester import DOMHarvester
    from core.browser.session import SessionManager
"""
# __all__ is empty — consumers must import submodules directly.
__all__: list = []
